import { Router } from 'express';

const router = Router();

// Placeholder for invoice routes
router.get('/', (req, res) => res.status(501).json({ message: 'Invoice routes not implemented yet.' }));

export default router;
