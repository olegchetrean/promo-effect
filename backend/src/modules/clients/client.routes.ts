import { Router } from 'express';

const router = Router();

// Placeholder for client routes
router.get('/', (req, res) => res.status(501).json({ message: 'Client routes not implemented yet.' }));

export default router;
