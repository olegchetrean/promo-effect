import { Router } from 'express';
const router = Router();

// TODO: Implement clients routes
router.get('/', (req, res) => res.json({ message: 'Clients API - Coming soon' }));

export default router;
